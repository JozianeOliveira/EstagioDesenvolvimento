// Cálculo do Valor de SOMA

const INDICE = 12;
let SOMA = 0;
let K = 1;

while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

// Exibir o valor de SOMA
console.log(`O valor da variável soma é ${SOMA}`);
